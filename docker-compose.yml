version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imcreactform
    image: yknam/imcreactform
    volumes:
      - ./src:/app/src
    ports:
      - "3080:80"
